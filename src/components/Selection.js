import React from "react";

const Selection = ({ color }) => {
  return (
    <div
      className="fix-box"
      style={{ backgroundColor: color }}
    ></div>
  );
};

export default Selection;



